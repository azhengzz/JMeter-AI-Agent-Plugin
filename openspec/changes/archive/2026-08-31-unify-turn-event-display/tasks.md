# Tasks: unify-turn-event-display

> 硬门禁（每任务组结束时）：`mvn clean test` 全绿（clean 防 stale class）。每段独立 commit 可 revert。契约不变式：`processMessage` future 语义、IPC wire 信封、DelegationGuard ack 窗口、InjectionManager 时序、`sessionEpochs`/`resetFenceLock`、`ipc-turn-gui-display` 7 条 Requirements 全程保持。

## 1. P0 — 事件流加法（无行为变化）

- [x] 1.1 新建 `agent.presenter` 5 类型：`TurnOrigin`/`CancelCause`/`TurnHandle`（SEQ 进程唯一、`terminalEmitted` 原子位、`visibleToPanel()`）/`TurnEvent`（7 Kind + origin 独立字段）/`TurnSubscriber`（javadoc 含线程不保证/锁上下文 O(微秒)/异常隔离/零跳可重入四条契约）。验证：`mvn clean test-compile`
- [x] 1.2 `AgentLoop`：`turnSubscribers` + `dispatchTurnEvent`（三守卫：空订阅 no-op / sessionKey≠当前实例键不派发 / 逐订阅者吞异常）+ `activeTurnHandles` + `activeTurn()`/`addTurnSubscriber`/`removeTurnSubscriber`；`startTurn(+origin)` 建 handle 并落 7 个发射点（终态在 try/catch 内、`future.complete` 之前、finally-republish 之前——D2 重排）；`signalCancel(key,cause)` 重载（单参保留 shim）；Phase1/2 内联点发 COMMAND_RESULT/INJECTED/REJECTED_BUSY。旧 `notify*` 并存不动。验证：`mvn clean test` 全绿且 src/test 零改动
- [x] 1.3 `AgentLoopFactory` 静态订阅表 + 挂接语义（`addTurnSubscriber` 记全局表并挂当前存活单例；`getAgentLoop`/`createAgentLoop` 创建时挂接；`reset()` 不清空表 + `clearTurnSubscribersForTest()`）。验证：`mvn clean test -Dtest=AgentLoopFactoryTest`（如有）后全量
- [x] 1.4 新增 `AgentLoopTurnEventTest`：回合内严格序 / 跨回合终态先于下回合 STARTED / 孤儿 STARTED 晚于垂死终态（含 reset 路径：RESET 取消在 resetFenceLock 内发射、订阅者早退不挂死）/ tryClaimTerminal 双发射点恰好一次 / 三守卫 / commandTurn+IPC 只发终态 / COMMAND_RESULT Phase1/Phase2 两点 / 先有 loop 后有面板挂接链路。验证：新测试全绿
- [x] 1.5 新增 `EventParityTest`（双通道影子等价）：Kind 序列等价 + 载荷映射表（`"agent failed: "` 前缀归一）+ 显式排除（InterruptedException 旧通知 / fromIpc 门控差异）；超时路径按事件序列断言非墙钟；两条竞态钉子（Enter 在途横跨自然完成、cleanup→孤儿 register 间隙——断言双回合回复都渲染 = 今日基线）。验证：全绿
- [x] 1.6 P0 commit

## 2. P1a — 换轨一：IPC/孤儿/命令事件源

- [x] 2.1 `AiChatPanel implements TurnSubscriber`：`onTurnEvent`（G1 零跳 + gen 快照）+ `dispatch` switch（LOCAL 源回合事件丢弃——本地路径暂留 worker；INJECTED 仅渲染非 LOCAL 源，LOCAL 源仍面板自渲染）；构造器 `AgentLoopFactory.addTurnSubscriber(this)`；`adoptRunningIpcTurnIfNeeded` 改 `activeTurn()` 查询 + `liveTurnIds.add(handle.id())` + armActiveTurn + 提示行（Q12 不补放）。验证：`mvn clean test -Dtest='AiChatPanelIpcTurnPresenterTest'`
- [x] 2.2 `IpcServer` 瘦身：删 turnPresented 判据与 6 处 notify* 转发腿（回调瘦身为只喂 accumulator）；`processMessageFromIpc`→4 参 origin 重载；超时分支 `cancelActiveTask(session, TIMEOUT)`；ExecutionException/CE/InterruptedException 分支不再通知（wire 200/500/409 不动）。验证：`mvn clean test -Dtest='IpcServer*Test,AgentLoopDelegationGuardTest'` 后全量
- [x] 2.3 删 `republishListener` 通道（AgentLoop 字段/setter/WARN 分支 + 面板 `registerRepublishListener` 整方法与两处调用）；孤儿回合 = REPUBLISH 源事件回合。验证：全量测试
- [x] 2.4 测试迁移（P1a 批）：`AgentLoopPresenterTest` 接口形态（localInject 断言暂保持旧语义——门控未删）；`AgentLoopRepublishTest` 15/15 用例订阅化（前置机械变更：`SESSION_KEY` 改 `InstanceContext.currentSessionKey()`）；`AgentLoopAdversarialTest` 孤儿系 + `nullListener_republishStillProcesses` + `queueFull_21stMessage` 改事件形态断言（reeGuard 保 executorService 反射与 error 终态意图）；`AiChatPanelIpcTurnPresenterTest` setUp 换 `loop.addTurnSubscriber(panel)`（:81-83 竞态等待保留；领养用例断言"领养后 PROGRESS/终态照常渲染"）；`AiChatPanelNewConversationTest.panel()` 反射 invoke（:532）改订阅接线（直建 loop 显式 addTurnSubscriber）。验证：全量 + `git diff` 审查断言语义一一对应
- [x] 2.5 P1a commit

## 3. P1b — 换轨二：本地路径

- [x] 3.1 `submitToLoop` 落地：替换 `startNormalSend` 武装段/worker 构造、`injectMessage` 同步回执+嗅探+竞态分支、`handleNewCommand` 手绘 You 行（→ gen++/清屏/submitToLoop("/new")）；`sendMessage` 收缩（hasActiveRun 预路由删）；`stopActiveTask` 收缩为 signalCancel(USER_STOP)+后台等待，**保留无条件 removeLoadingIndicator+setButtonToSendMode**；活回合集合过滤（`liveTurnIds`）全面接管（含 TURN_STARTED/PROGRESS/终态分支）。验证：`mvn clean test -Dtest='NewCommandCancelTest,AiChatPanelNewConversationTest'` 后全量
- [x] 3.2 删 `fromIpc` 门控（INJECTED 无条件发射）+ 面板 INJECTED 自渲染删（P1a 的 LOCAL 源丢弃分支删）；`localInjectWhileBusyDoesNotNotifyPresenter` 断言显式反转为"本地注入必通知恰一次"；F3 用例改 turnId 驱动语义。验证：全量
- [x] 3.3 删 `AgentSwingWorker.java` + 面板 `activeWorker` 字段与引用；`handleProgressNow` 去 invokeLater 双跳。验证：全量 + grep 无残留引用
- [x] 3.4 P1b commit

## 4. P2 — 清扫与固化

- [x] 4.1 删旧 `notify*` 六方法/`setTurnPresenter`/`getTurnPresenter`/`setRepublishListener`/`processMessageFromIpc`；**保留** D5 兼容 shim（`processMessage(msg,key,boolean)`、`(msg,key,callback,boolean)`、`signalCancel(String)`、`cancelActiveTask(String)`——委托不 fork，调用点清单见 design D5）；删 `TurnPresenter.java`。验证：全量 + `git diff` 确认 shim 全是单行委托（门禁初跑红一次：`@TempDir` 清理与后台回合会话写盘竞态——4.1 更快的 EDT 渲染路径提早满足断言放大既有窗口；tearDown 有界排空 `activeTurn(sessionKey)` 后全绿 457/0/0）
- [x] 4.2 测试卫生：构造过 AiChatPanel 的测试 tearDown 补 `AgentLoopFactory.removeTurnSubscriber(panel)`；新增"模型切换后面板订阅仍存活"用例（`switchAiService` reset→重建路径）。验证：全量（重复跑两遍确认无跨用例污染）（两遍 458/0/0 BUILD SUCCESS）
- [x] 4.3 `MessageProcessor` 入口加 `EventQueue.isDispatchThread()` 断言（迁移期护栏）；CLAUDE.md 架构段更新（AgentLoop 条目 + 4 处 UX 差异 + SwingWorker 删除决策 + SILENT 显示域）。验证：`git diff` 审查（护栏即刻抓获真实违规：面板构造线程调 `displayWelcomeMessage` 渲染欢迎消息——已修为 EDT 同步/自投两分支；受影响测试 34/0/0）
- [x] 4.4 **turn-centric 文档登记（两项修订为交付物）**：`refactor-agent-loop-turn-centric` proposal/tasks 门禁基线改写为本变更 P2 后测试形态（本变更 = 其 Phase 0）；其 design.md D1 字段表补 `TurnHandle`/`activeTurnHandles`/`terminalEmitted` 与"终态先于 finally-republish、Turn 聚合不得绕过 dispatchTurnEvent"不变式。验证：`openspec validate refactor-agent-loop-turn-centric`（输出 valid；顺带修正其 design 两处陈旧基线表述：presenter 通知→回合事件流、SwingWorker 消费→订阅端自投 EDT）
- [x] 4.5 P2 commit

## 5. 收尾验收

- [ ] 5.1 全量 `mvn clean test` + `mvn clean install`（JMETER_HOME 就绪，lib/ext 只留最新版 jar）✅ 自动部分已过（458→462/0/0 全绿 + install BUILD SUCCESS + lib/ext 单 jar）；**手动冒烟待用户执行**：本地回合全流、busy 注入（本地+CLI 双源）、delegated busy 拒、Stop（本地/IPC 回合/无目标窗口）、/new idle 与 mid-run、模型切换重建后 CLI 回合显示与孤儿回复、双实例委派超时 504、面板懒创建领养、关闭整合取消（LOCAL 无噪音行 / IPC 保留回执行）
- [x] 5.2 对照 spec `agent-turn-events` 逐条 Requirement 复核（EventParityTest 两条竞态钉子 + TurnEventTest 顺序/去重用例映射）；对照 proposal"4 处 UX 差异"复核无第 5 处漂移（复核方式：10 项工作流逐 Requirement + ux-drift 审查 + 对抗性复核；结论：9 项合规、1 项 ux-drift 疑点被驳回（agentLoop==null 路径为已文档化 D5 删除决策）、2 项确认缺口已修复——①生产缺口：CloseConsolidationDialog 用单参 cancelActiveTask 致 `CancelCause.SILENT` 死值（design D5 :127 原计划的 1 行改动在 tasks 拆分中遗漏），已改两参调用；②测试缺口：TIMEOUT/SILENT cause 谱系、SILENT/RESET 显示域、COMMAND_RESULT 显示域、Stop 无可取消对象无条件复位均无钉子，已补 4 测试（AgentLoopTurnEventTest +1 / AiChatPanelIpcTurnPresenterTest +3）。门禁 462/0/0）
- [x] 5.2b 交付后对抗性代码评审（7 维度工作流，30 项确认发现）分三批修复：批 1（dispatchTurnEvent 兜 Throwable / @Deprecated 测试脚手架 shim / buildProgressHook 去订阅表门控 / loading 武装位 / TurnEvent.origin() 解析自 turn）；批 2（取消路径倒序契约修订——TurnSubscriber 顺序契约 + spec 新增「取消路径倒序容忍」Scenario + 面板倒序钉子；C3 工厂路由 signalCancelAny：换血后 Stop 经当前+退役 loop 路由）；批 3（cleanup 摘订阅/置空 INSTANCE、TurnOrigin 穷尽 switch 谓词 isLocalPanel()/isIpcPeer()、appendCancelLine 谓词收敛 `!origin.isIpcPeer()`（SILENT+REPUBLISH 不再误渲染，补面板钉子）、handleAgentResponse 死代数参数删除、advanceRenderEpoch() 三处提取、TurnHandle cancelCause 死字段删除（cause 经事件载荷，6 处文档同步）、测试脚手架收编 agent.testsupport（GatedScriptAiService 含 InterruptStrategy.HANG_UNTIL_RELEASED / RecordingSubscriber / NoopTool / AwaitUtil）、注释考古清理。门禁 464→466→466/0/0）
- [x] 5.2c 交付后全面对抗性测试（79-agent 工作流：6 视角攻击 → 双复核严格 AND → 证伪测试设计；26 发现 → **20 确认 / 5 边缘 / 1 驳回**，全文 `d:/tmp/adv-findings.md`，设计稿 `d:/tmp/adv-designs/`）。落地 29 证伪/压力测试（5 新类：CancelRaceInvariant/WaitForCancellation/AdversarialStress/IpcServerAgentEnvelopeWire/IpcServerAgentTimeoutRace + TurnEventTest/PresenterTest 追加）：**19 项重现已确认缺陷首跑 RED 留证 `@Disabled`**（注解含缺陷编号与转绿条件；取证断言消息如「turn#408 terminal=0」「expected 1 but was 3 截断文件被复活」「expected 200 but was 504」），10 项 GREEN 保持 enabled（IPC 信封 wire 四态全路径、R6 迟到终态 AND 子句、waitForCancellation 自然路径守卫表、取消竞态自洽守卫、3 套固定种子压力套件：取消风暴 0xC0FFEE/换血风暴 0xBEEF/混合源风暴 0x5EED）。门禁 495/0/0（skip 30 = 11 基线 + 19 留证）。确认缺陷簇：退役 loop 取消路由缺口（3/4/5/9/17，critical×2）、loading 指示无回合身份（1/6/11/18）、signalCancel 句柄/future 错配（0）、waitForCancellation 架空（15）等——修复授权待用户决策
- [x] 5.2d 修复全部 20 项确认缺陷（用户授权「全部 20 项（推荐）」，沿用分批模式，每批过门禁、留证测试逐批摘 `@Disabled` 验证转绿；19 留证 → 0）。批 2/3（面板+IpcServer 侧 11 留证）：幂等武装 `loadingIndicatorArmed`（单指示器不变式，修 1/6/11/18）、指示器移除与按钮复位改判据 `liveTurnIds.isEmpty()`（修 2/10 退役/拆窗误判与跨回合吞并）、per-turn `progressiveToolCallTurnIds`（修 8 跨回合工具摘要）、领养守卫 `visibleToPanel()`+`terminalEmitted()`（修 7/16 死回合领养）、远程 `/new` 清屏双路径（TURN_COMPLETED 的 echoText 空闲 Phase3 + COMMAND_RESULT 的 message 忙期 Phase2，修 14）、IpcServer 超时分支 cancel 后 `isDone&&!isCancelled` 复查回 200 成功信封（修 19 TOCTOU）。批 1+簇 C（AgentLoop/Factory 侧 8 留证，3 设计师+1 裁决工作流定稿）：[15] completionLatch 释放点从 `whenComplete`（被 `future.cancel` 取消线程同步触发架空等待）迁至 executor lambda 外层 finally（结构性穷尽 guard return/正常尾/异常/内层 finally 逃逸，按值摘防误摘后继回合 latch）+ REE 分支同步补释放，`waitForCancellation`/`cancelActiveTask` 语义自动变真（IpcServer 304 与关闭对话框免费受益）；[0] `signalCancel` 摘 future 后、cancel 前重读句柄再认领（同会话回合线性序保证与被取消 future 同回合，消灭「取消 O 的 future、认领 N 的句柄 → O 零终态卡死面板」）；簇 A 路由 `AgentLoopFactory.signalCancelRouted(key,cause,exclude)`（自 `signalCancelAny` 提炼，exclude 语义）+ `resetConversationAny(self,key[,archive])`（RESET 先路由触达当前+退役 loop 在跑回合——abort 置位程序序先于 self 腿截断落盘，迟到收尾落盘复查放弃写盘，修 3/4/5/9/17 落盘复活与漏取消；self 参数保直构 loop/命令回合身份豁免语义，NewCommandCancelTest 等回归面存活）+ `cancelActiveTaskAny(key,cause)`（先捕获目标集再取消再共享 5s deadline 逐 loop 等收尾，null-safe 覆盖重建窗口 instance==null）；4 生产调用点换路由（`BuiltinCommands.cmdNew`/`AiChatPanel.startNewConversation`/`CloseConsolidationCoordinator.cancelActiveTurnsSilently` 新入口+`clearCurrentSession` 尾句/`CloseConsolidationDialog.doInBackground` 整块替换，ISE 吞取消路径物理消亡）；测试侧 `dialogCancelStep` 按内嵌提示换协调器真入口、测试 12 换 `resetConversationAny`。spec.md +2 Scenario（「重置与静默取消跨 loop 路由」「收尾等待以回合体收尾为准」）。门禁 495/0/0（skip 19→11，19 留证全数摘除转绿）
- [x] 5.3 `openspec archive` 归档变更
