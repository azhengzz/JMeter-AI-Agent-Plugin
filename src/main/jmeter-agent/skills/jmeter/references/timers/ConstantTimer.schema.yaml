component:
  type: constanttimer
  name: Constant Timer
  description: Adds a fixed pause before each sampler in its scope

properties:
  - name: ConstantTimer.delay
    type: String
    required: true
    default: "300"
    description: Delay in milliseconds
